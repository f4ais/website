@extends('layouts.app')
@section('title', 'Proses Penyaluran')
@section('page-title', 'Proses Penyaluran Bantuan')
@section('content')
<div class="grid gap-6 lg:grid-cols-[1fr_1.2fr]"><section class="h-fit rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p class="text-xs font-semibold uppercase tracking-wider text-emerald-700">{{ $recipient->program->code }}</p><h2 class="mt-1 text-xl font-bold">{{ $recipient->citizen->name }}</h2><p class="mt-1 text-sm text-slate-500">NIK {{ $recipient->citizen->nik }}</p><dl class="mt-6 space-y-4 text-sm"><div><dt class="text-slate-500">Program</dt><dd class="font-semibold">{{ $recipient->program->name }}</dd></div><div><dt class="text-slate-500">Alamat</dt><dd class="font-semibold">{{ $recipient->citizen->address }}</dd></div><div><dt class="text-slate-500">Wilayah</dt><dd class="font-semibold">{{ $recipient->citizen->wilayah }}</dd></div><div><dt class="text-slate-500">Skor prioritas</dt><dd class="text-2xl font-black text-emerald-800">{{ $recipient->citizen->latestSurvey?->priority_score ?? '—' }}</dd></div><div><dt class="text-slate-500">Status saat ini</dt><dd class="mt-1"><x-status-badge :status="$recipient->status" /></dd></div></dl></section>
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-bold">Update Penyaluran</h2><p class="mt-1 text-sm text-slate-500">Foto dokumentasi wajib ketika status tersalurkan.</p><form action="{{ route('penyalur.distributions.update',$recipient) }}" method="POST" enctype="multipart/form-data" class="mt-6 space-y-5">@csrf @method('PUT')<div><label class="mb-2 block text-sm font-semibold">Status bantuan</label><select name="status" required class="w-full rounded-xl border border-slate-300 px-4 py-3">@foreach(['diproses'=>'Dalam proses','tersalurkan'=>'Tersalurkan','gagal'=>'Gagal disalurkan'] as $value=>$label)<option value="{{ $value }}" @selected(old('status',$recipient->distribution?->status ?? $recipient->status)===$value)>{{ $label }}</option>@endforeach</select></div><div><label class="mb-2 block text-sm font-semibold">Tanggal dan waktu penyaluran</label><input type="datetime-local" name="distributed_at" value="{{ old('distributed_at',optional($recipient->distribution?->distributed_at)->format('Y-m-d\TH:i')) }}" class="w-full rounded-xl border border-slate-300 px-4 py-3"></div><div><label class="mb-2 block text-sm font-semibold">Foto dokumentasi</label><input type="file" name="documentation_photo" accept="image/*" class="w-full rounded-xl border border-slate-300 px-4 py-3 text-sm"></div><div><label class="mb-2 block text-sm font-semibold">Catatan penyaluran</label><textarea name="notes" rows="4" class="w-full rounded-xl border border-slate-300 px-4 py-3">{{ old('notes',$recipient->distribution?->notes) }}</textarea></div>@if($recipient->distribution?->documentation_photo)<div><p class="mb-2 text-sm font-semibold">Dokumentasi tersimpan</p><img src="{{ Storage::url($recipient->distribution->documentation_photo) }}" class="max-h-72 w-full rounded-xl object-cover" alt="Dokumentasi penyaluran"></div>@endif<button class="rounded-xl bg-emerald-800 px-5 py-3 font-semibold text-white">Simpan Perubahan</button></form></section></div>
@endsection
