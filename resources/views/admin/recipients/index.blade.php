@extends('layouts.app')
@section('title', 'Penerima Bantuan')
@section('page-title', 'Penetapan Penerima Bantuan')
@section('content')
<div class="grid gap-6 xl:grid-cols-[420px_1fr]"><section class="h-fit rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="text-lg font-bold">Tetapkan Penerima</h2><p class="mt-1 text-sm text-slate-500">Hanya warga yang sudah terverifikasi yang dapat dipilih.</p><form action="{{ route('admin.recipients.store') }}" method="POST" class="mt-5 space-y-4">@csrf<div><label class="mb-2 block text-sm font-semibold">Program aktif</label><select name="aid_program_id" required class="w-full rounded-xl border border-slate-300 px-4 py-3"><option value="">Pilih program</option>@foreach($programs as $program)<option value="{{ $program->id }}">{{ $program->name }} ({{ $program->recipients_count }}/{{ $program->quota }})</option>@endforeach</select></div><div><label class="mb-2 block text-sm font-semibold">Warga terverifikasi</label><select name="citizen_id" required class="w-full rounded-xl border border-slate-300 px-4 py-3"><option value="">Pilih warga</option>@foreach($eligibleCitizens as $citizen)<option value="{{ $citizen->id }}">Skor {{ $citizen->latestSurvey?->priority_score ?? 0 }} — {{ $citizen->name }} — {{ $citizen->wilayah }}</option>@endforeach</select></div><div><label class="mb-2 block text-sm font-semibold">Catatan</label><textarea name="notes" rows="3" class="w-full rounded-xl border border-slate-300 px-4 py-3"></textarea></div><button class="w-full rounded-xl bg-emerald-800 px-4 py-3 font-semibold text-white">Tetapkan Penerima</button></form></section>
<section><div class="mb-4 flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-bold">Daftar Penerima</h2><p class="text-sm text-slate-500">Penerima yang telah ditetapkan oleh admin.</p></div><form><select name="program" onchange="this.form.submit()" class="rounded-xl border border-slate-300 px-4 py-2.5"><option value="">Semua program</option>@foreach($programs as $program)<option value="{{ $program->id }}" @selected((string)request('program')===(string)$program->id)>{{ $program->name }}</option>@endforeach</select></form></div><div class="space-y-3">@forelse($recipients as $recipient)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-wider text-emerald-700">{{ $recipient->program->code }}</p><h3 class="mt-1 font-bold">{{ $recipient->citizen->name }}</h3><p class="mt-1 text-sm text-slate-500">{{ $recipient->citizen->wilayah }} • Skor {{ $recipient->citizen->latestSurvey?->priority_score ?? '—' }}</p></div><div class="flex items-center gap-2"><x-status-badge :status="$recipient->status" />@if($recipient->status!=='tersalurkan')<form action="{{ route('admin.recipients.destroy',$recipient) }}" method="POST" onsubmit="return confirm('Batalkan penetapan ini?')">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 px-3 py-2 text-sm text-rose-700">Batalkan</button></form>@endif</div></div></div>@empty<x-empty-state message="Belum ada penerima yang ditetapkan." />@endforelse</div><div class="mt-5">{{ $recipients->links() }}</div></section></div>
@endsection
