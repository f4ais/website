@extends('layouts.app')
@section('title', 'Laporan')
@section('page-title', 'Laporan Bantuan Sosial')
@section('content')
<div class="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-bold">Ringkasan Sistem</h2><p class="text-sm text-slate-500">Rekap pendataan, verifikasi, penetapan, dan penyaluran.</p></div><a href="{{ route('admin.reports.export') }}" class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-semibold text-white">Unduh CSV</a></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5"><x-stat-card label="Warga" :value="$summary['warga']"/><x-stat-card label="Terverifikasi" :value="$summary['verified']"/><x-stat-card label="Ditolak" :value="$summary['rejected']"/><x-stat-card label="Penerima" :value="$summary['recipients']"/><x-stat-card label="Tersalurkan" :value="$summary['distributed']"/></div>
<div class="mt-8 grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-bold">Rekap per Program</h2><div class="mt-4 space-y-3">@forelse($programs as $program)<div class="rounded-xl bg-slate-50 p-4"><div class="flex justify-between gap-3"><div><p class="font-semibold">{{ $program->name }}</p><p class="text-xs text-slate-500">{{ $program->code }}</p></div><p class="font-bold">{{ $program->recipients_count }}/{{ $program->quota }}</p></div><div class="mt-3 h-2 overflow-hidden rounded-full bg-slate-200"><div class="h-full rounded-full bg-emerald-700" style="width: {{ min(($program->recipients_count/max($program->quota,1))*100,100) }}%"></div></div></div>@empty<x-empty-state message="Belum ada program." />@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 class="font-bold">Sebaran Warga per Wilayah</h2><div class="mt-4 divide-y divide-slate-100">@forelse($regions as $region)<div class="flex items-center justify-between py-3"><span>{{ $region->wilayah }}</span><span class="rounded-lg bg-emerald-100 px-3 py-1 font-bold text-emerald-900">{{ $region->total }}</span></div>@empty<x-empty-state message="Belum ada data wilayah." />@endforelse</div></section></div>
@endsection
