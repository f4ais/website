@extends('layouts.app')
@section('title', 'Detail Verifikasi')
@section('page-title', 'Detail Hasil Verifikasi')
@section('content')
<div class="grid gap-6 lg:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-semibold uppercase tracking-wider text-emerald-700">Data warga</p><h2 class="mt-1 text-2xl font-bold">{{ $survey->citizen->name }}</h2><p class="mt-1 text-sm text-slate-500">NIK {{ $survey->citizen->nik }}</p></div><x-status-badge :status="$survey->citizen->verification_status" /></div><dl class="mt-6 grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-slate-500">Wilayah</dt><dd class="font-semibold">{{ $survey->citizen->wilayah }}</dd></div><div><dt class="text-slate-500">Tanggungan awal</dt><dd class="font-semibold">{{ $survey->citizen->dependents }}</dd></div><div><dt class="text-slate-500">Penghasilan awal</dt><dd class="font-semibold">Rp{{ number_format($survey->citizen->income,0,',','.') }}</dd></div><div><dt class="text-slate-500">Kondisi rumah awal</dt><dd class="font-semibold capitalize">{{ str_replace('_',' ',$survey->citizen->house_condition) }}</dd></div><div class="sm:col-span-2"><dt class="text-slate-500">Alamat</dt><dd class="font-semibold">{{ $survey->citizen->address }}</dd></div></dl></section>
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-start justify-between"><div><p class="text-xs font-semibold uppercase tracking-wider text-emerald-700">Hasil lapangan</p><h2 class="mt-1 text-xl font-bold">{{ $survey->surveyor->name }}</h2></div><x-status-badge :status="$survey->status" /></div>@if($survey->status==='assigned')<div class="mt-6"><x-empty-state message="Surveyor belum mengunggah hasil verifikasi." /></div>@else<div class="mt-6 grid gap-4 text-sm sm:grid-cols-2"><div><p class="text-slate-500">Penghasilan terverifikasi</p><p class="font-bold">Rp{{ number_format($survey->verified_income,0,',','.') }}</p></div><div><p class="text-slate-500">Tanggungan terverifikasi</p><p class="font-bold">{{ $survey->verified_dependents }}</p></div><div><p class="text-slate-500">Kondisi rumah</p><p class="font-bold capitalize">{{ str_replace('_',' ',$survey->verified_house_condition) }}</p></div><div><p class="text-slate-500">Skor prioritas</p><p class="text-3xl font-black text-emerald-800">{{ $survey->priority_score ?? '—' }}</p></div><div class="sm:col-span-2"><p class="text-slate-500">Catatan surveyor</p><p class="mt-1 leading-6">{{ $survey->notes }}</p></div>@if($survey->evidence_photo)<div class="sm:col-span-2"><p class="mb-2 text-slate-500">Bukti survei</p><img src="{{ Storage::url($survey->evidence_photo) }}" class="max-h-80 w-full rounded-xl object-cover" alt="Bukti survei"></div>@endif</div>@endif</section></div>
<div class="mt-5"><a href="{{ route('admin.verifications.index') }}" class="rounded-xl border border-slate-300 px-4 py-2.5 text-sm font-semibold">Kembali</a></div>
@endsection
