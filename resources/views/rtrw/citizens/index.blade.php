@extends('layouts.app')
@section('title', 'Data Warga Wilayah')
@section('page-title', 'Data Warga Wilayah')
@section('content')
<div class="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-bold">{{ auth()->user()->wilayah }}</h2><p class="text-sm text-slate-500">Anda tidak dapat melihat atau mengubah data wilayah lain.</p></div><a href="{{ route('rtrw.citizens.create') }}" class="rounded-xl bg-emerald-800 px-4 py-2.5 text-sm font-semibold text-white">Input Data Warga</a></div>
<form class="mb-5 flex gap-3 rounded-2xl border border-slate-200 bg-white p-4"><input name="search" value="{{ request('search') }}" placeholder="Cari nama atau NIK..." class="min-w-0 flex-1 rounded-xl border border-slate-300 px-4 py-2.5"><button class="rounded-xl border border-slate-300 px-4 py-2.5 font-semibold">Cari</button></form>
<div class="grid gap-4 lg:grid-cols-2">@forelse($citizens as $citizen)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div><h3 class="font-bold">{{ $citizen->name }}</h3><p class="mt-1 text-xs text-slate-500">NIK {{ $citizen->nik }}</p></div><x-status-badge :status="$citizen->verification_status" /></div><div class="mt-4 grid grid-cols-2 gap-3 rounded-xl bg-slate-50 p-4 text-sm"><div><p class="text-xs text-slate-500">Penghasilan</p><p class="font-semibold">Rp{{ number_format($citizen->income,0,',','.') }}</p></div><div><p class="text-xs text-slate-500">Tanggungan</p><p class="font-semibold">{{ $citizen->dependents }} orang</p></div></div><div class="mt-4 flex gap-2"><a href="{{ route('rtrw.citizens.status',$citizen) }}" class="rounded-lg bg-emerald-50 px-3 py-2 text-sm font-semibold text-emerald-800">Lihat Status</a>@if($citizen->verification_status!=='verified')<a href="{{ route('rtrw.citizens.edit',$citizen) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-sm">Edit</a>@endif</div></div>@empty<div class="lg:col-span-2"><x-empty-state message="Belum ada warga di wilayah ini." /></div>@endforelse</div><div class="mt-5">{{ $citizens->links() }}</div>
@endsection
