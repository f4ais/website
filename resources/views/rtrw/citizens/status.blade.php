@extends('layouts.app')
@section('title', 'Status Verifikasi')
@section('page-title', 'Status Verifikasi Warga')
@section('content')
<div class="grid gap-6 lg:grid-cols-[1fr_1.3fr]"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="flex items-start justify-between gap-3"><div><h2 class="text-xl font-bold">{{ $citizen->name }}</h2><p class="mt-1 text-sm text-slate-500">NIK {{ $citizen->nik }}</p></div><x-status-badge :status="$citizen->verification_status" /></div><dl class="mt-6 space-y-3 text-sm"><div><dt class="text-slate-500">Alamat</dt><dd class="font-medium">{{ $citizen->address }}</dd></div><div><dt class="text-slate-500">Wilayah</dt><dd class="font-medium">{{ $citizen->wilayah }}</dd></div><div><dt class="text-slate-500">Penghasilan input</dt><dd class="font-medium">Rp{{ number_format($citizen->income,0,',','.') }}</dd></div></dl></section><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-bold">Riwayat Verifikasi</h2><div class="mt-5 space-y-4">@forelse($citizen->surveys as $survey)<div class="rounded-xl border border-slate-200 p-4"><div class="flex items-center justify-between"><p class="font-semibold">{{ $survey->surveyor->name }}</p><x-status-badge :status="$survey->status" /></div><p class="mt-2 text-sm text-slate-500">Jadwal: {{ optional($survey->scheduled_at)->format('d M Y H:i') ?? 'Belum ditentukan' }}</p>@if($survey->surveyed_at)<p class="mt-1 text-sm text-slate-500">Selesai: {{ $survey->surveyed_at->format('d M Y H:i') }}</p><p class="mt-3 text-sm">{{ $survey->notes }}</p>@endif</div>@empty<x-empty-state message="Belum ada surveyor yang ditugaskan." />@endforelse</div></section></div>
@endsection
