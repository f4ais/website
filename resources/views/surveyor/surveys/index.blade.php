@extends('layouts.app')
@section('title', 'Daftar Survei')
@section('page-title', 'Daftar Tugas Survei')
@section('content')
<div class="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 class="text-xl font-bold">Tugas Verifikasi Lapangan</h2><p class="text-sm text-slate-500">Anda hanya dapat melihat dan memproses tugas yang diberikan kepada Anda.</p></div><form><select name="status" onchange="this.form.submit()" class="rounded-xl border border-slate-300 px-4 py-2.5"><option value="">Semua status</option>@foreach(['assigned','verified','rejected'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></form></div>
<div class="grid gap-4 lg:grid-cols-2">@forelse($surveys as $survey)<a href="{{ route('surveyor.surveys.show',$survey) }}" class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm transition hover:border-emerald-300"><div class="flex items-start justify-between gap-3"><div><h3 class="font-bold">{{ $survey->citizen->name }}</h3><p class="mt-1 text-xs text-slate-500">NIK {{ $survey->citizen->nik }}</p></div><x-status-badge :status="$survey->status" /></div><p class="mt-4 text-sm text-slate-600">{{ $survey->citizen->address }}</p><div class="mt-4 flex items-center justify-between border-t border-slate-100 pt-4 text-sm"><span class="text-slate-500">{{ $survey->citizen->wilayah }}</span><span class="font-semibold">{{ optional($survey->scheduled_at)->format('d M Y H:i') ?? 'Belum dijadwalkan' }}</span></div></a>@empty<div class="lg:col-span-2"><x-empty-state message="Tugas survei tidak ditemukan." /></div>@endforelse</div><div class="mt-5">{{ $surveys->links() }}</div>
@endsection
